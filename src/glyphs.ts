const gclef = '𝄞'
const bclef = '𝄢'
const half_note = '𝅘'
const dbarline = '𝄁'
const brace = '𝄔'

export type GlyphMap = {
  [key: string]: string
}

export default {
  gclef,
  bclef,
  half_note,
  dbarline,
  brace
} as GlyphMap
